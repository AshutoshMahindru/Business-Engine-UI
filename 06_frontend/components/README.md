# components

Reusable UI primitives used across features.

## Subfolders
- `core/` universal product components
- `data/` metric and table components
- `charts/` visualization components
- `layout/` shell, rails, grids, navigation
